#ifndef MATH_ACOS_H
#define MATH_ACOS_H
#include "types.h"
#include <stdio.h>

/* 
 * Currently only planning to support a single 
 * accuracy level and algorithm, but may add
 * variable accuracy and speed settings later.
 *
 * */



/* 
 * @brief Compute inverse Cos function in Radians
 *
 * @param theta Angle in radians
 * @return Returns a floating point value
 *
 * */
static inline float acos_r(radians theta) {
}

static inline float acos_d(degrees theta) {
}



#endif
